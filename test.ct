incluir <stdio.h>
incluir <stdlib.h>

funcao inteiro test (inteiro a) {
  inteiro variavel
  real teste
  caracter teste_2
}


funcao caracter test_2 () {
  inteiro contador
}

funcao_principal {
  inteiro i
  inteiro soma
  inteiro quantidade
  real retorno
  caracter funcoes

  i := soma
  soma := i
  soma++
  i++
  i--
  soma--
  soma := soma + soma
  i := soma - i
  i := soma * i
  soma := i / soma
}
